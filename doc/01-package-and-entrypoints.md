# Package and Entry Points

Use a simple `package` declaration and keep the script at repository-relative paths so it can be mounted under `/work` inside the runner container.

```text
package momot.search
```
