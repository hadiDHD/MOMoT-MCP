# Results and Output Layout

Write results into the mounted `/out` volume so the REST runner and MCP tools can collect them.

```text
results = {
	models = {
		outputDirectory = "out/models/"
	}
}
```
